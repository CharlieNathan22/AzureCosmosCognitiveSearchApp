@page "/searchcosmos"
@using AzureCosmosCognitiveSearchApp.CosmosAPI;
@using AzureCosmosCognitiveSearchApp.Models;

@inject ICosmosDBService CosmosDBService

<PageTitle>Search Cosmos</PageTitle>

<h1>Find a user by filtering and searching the Azure Cosmos DB</h1>

<p>
    <label>
        <input @bind-value="@SearchInput.Name">Search user by Name</input>
    </label>
    <label>
        <button @onclick="NameSearch">Search user by Name</button>
    </label>
</p>

@if(!hasSearched){
    <p>Click the search button to search the Cosmos DB</p>
}else
{
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Balance</th>
                <th>Region</th>
                <th>Postcode</th>
                <th>Created Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in users)
            {
                <tr>
                    <td>@user.Id</td>
                    <td>@user.Name</td>
                    <td>@user.WalletAmount</td>
                    <td>@user.Region</td>
                    <td>@user.Postcode</td>
                    <td>@user.CreatedDate</td>
                </tr>
            }
        </tbody>
    </table>
}